# Part 1 catenation, multiplication of strings
#def small_box_test():
plus = '+'
minus = ' - '
space1 = " "
space4 = '    '
pipe ='|'
print()
print(plus,minus*4,plus,minus*4,plus)
print(pipe,space4*4,pipe,space4*4,pipe)
print(pipe,space4*4,pipe,space4*4,pipe)
print(pipe,space4*4,pipe,space4*4,pipe)
print(pipe,space4*4,pipe,space4*4,pipe)
print(plus,minus*4,plus,minus*4,plus)
print(pipe,space4*4,pipe,space4*4,pipe)
print(pipe,space4*4,pipe,space4*4,pipe)
print(pipe,space4*4,pipe,space4*4,pipe)
print(pipe,space4*4,pipe,space4*4,pipe)
print(plus,minus*4,plus,minus*4,plus)
